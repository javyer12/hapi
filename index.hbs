<!-- Main jumbotron for a primary marketing message or call to action -->

<div class="jumbotron bg-white  bg-body rounded  help">
  <div class="container shadow bg-body rounded ">
    <h1 class="display-3">Pregunta y Responde</h1>
    <p>
      El mejor lugar para aprender y ayudar a quienes están aprendiendo, crea
      preguntas para resolver tus dudas y comparte tu conocimiento respondiendo
      si conoces del tema
    </p>
    {{#if user}}{{else}}
    <h5>Ingrese para <a href="/login">crear pregunta</a></h5>
    {{/if}}
  </div>
  {{!-- {{#if answer}}
  <div class="alert alert-success" role="alert">
    {{this}}
  </div>
  {{else}}
  <div class="alert alert-danger" role="alert">
    {{this}}
  </div>
  {{/if}} --}}
</div>

<div class="container ">
  <!-- Example row of columns -->
  <h2 class="mb-4">Últimas preguntas</h2>
  {{#each questions as |question key|}}
  <div class="card bg-white mb-3 help">
    <div class="card-body">
      <div class="row helping">
        <div class="col col-lg-2  col-sm-5 text-center">
          <a href="/question/{{key}}" class="text-dark">
            <h3>
              {{answerNumber question.answers}}
            </h3>
            Respuestas
          </a>
        </div>
        <div class="col8 col-lg-10 col-sm-7 align-items-stretch">
          <h4 class="mt-3">
            <a href="/question/{{key}}" class="text-dark">{{question.title}}</a>
          </h4>
        </div>
      </div>
    </div>
  </div>
  <hr />
  {{/each}}
</div>
<!-- /container -->